#include "main.h"

/**
 * _abs - entry point
 * @n: n is an integer
 * function computes the absolute value of an integer
 * Return: int
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
